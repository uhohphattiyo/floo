<div itemscope itemtype="http://schema.org/FoodEstablishment">
  <div id="places" class="transitions-enabled">
    <% @places.each do |place| %>     
        <div class="box">

          <div id="image-wrapper">
            <span itemprop="photo"><%= link_to image_tag(place.image.url(:medium)), place, class: "hover" %>
            </span>
    
           <span class="image-text"> 
              <span itemprop="name">
                <%= place.name %>
              </span>
  
              <span itemprop="servesCuisine"><%= ["beer", "chocolate" ,"cocktail", "coffee", "tea", "wine", "juice"] [place.kind] %>
              </span>
          </span>
        
            
           <% if place.admin_user == current_admin_user %>
              <div class="actions">
                <%= link_to 'Edit', edit_place_path(place) %>
                <%= link_to 'Destroy', place, method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
          <% end %>
        </div>
      
      </div>
    <% end %>
  </div>
</div>

<div class="center">
   <%= will_paginate @places, renderer: BootstrapPagination::Rails %>
</div>