<%= render 'pages/home' %>

<div id="places" class="transitions-enabled">
<% @cities.each do |city| %>     
    
     <div class="box">   
        
        <%=link_to( image_tag(city.image.url(:medium)), places_path, :class=>"city") %>

           <% if city.admin_user == current_admin_user %>
            <div class="actions">
                <%= link_to 'Edit', edit_city_path(city) %>
                <%= link_to 'Destroy', city, method: :delete, data: { confirm: 'Are you sure?' } %>
            </div>
            <% end %>
      </div>
    
<% end %>
</div>