<div class="row">

	 <%= render 'pages/home' %> 
</div>

<div class="row">
<div class="col-md-12">
		<div id="cities" class="transitions-enabled">
		<% @kinds.each do |city| %>     



		     <div class="box">   
		        

			   
			   
		           <% if kind.user == current_user %>
		            <div class="actions">
		                <%= link_to 'Edit', edit_kind_path(kind) %>
		                <%= link_to 'Destroy', kind, method: :delete, data: { confirm: 'Are you sure?' } %>
		            </div>
		            <% end %>
		      </div>


		<% end %>
		</div>
</div>


<div class="blackout"></div>
</div>


									
									
									
</div>